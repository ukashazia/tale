# Tale 1.0 acceptance evidence

The acceptance integration tests cover deterministic artifact generation,
doctor redaction, and the release artifact contract. The complete fifteen-row
evidence matrix is in `journeys.md`; journeys that require a real Tailscale
daemon, Control API credential, terminal emulator, or platform are recorded as
blocked manual evidence and are never reported as passed by mock tests.
